{
  "name": "greet-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "Golden-eval fixture: empty starting point; the firm must add greet() + a test.",
  "scripts": {
    "test": "node --test"
  }
}
